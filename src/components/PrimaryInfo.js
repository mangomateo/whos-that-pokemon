
const PrimaryInfo = (props) => {
    return (
        <div id="info-1">
            <p>Base Exp: {props.baseExp}</p>
            <p>Type: {props.type}</p>
        </div>
    );
}

export default PrimaryInfo;