
const SecondaryInfo = (props) => {
    return (
        <div id="info-2">
            <p>Type: {props.type}</p>
            <p>Abilities: {props.ability}</p>
        </div>
    );
}

export default SecondaryInfo;